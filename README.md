# VisitTrack (Lightweight)
This is a simple lightweight visitor tracker (no database).

## How to deploy on Render
1. Create a new GitHub repository and push these files.
2. On Render, create a new Web Service and connect your GitHub repo.
3. Set the start command to: `python app.py`
4. After deployment, open `/send` for senders and `/map` for manager view.
